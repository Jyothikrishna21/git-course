get the user data....

git tag is used for releasing the versions of the software

git tag -a <version name or number> -m ""

git tag is used for checking the tags

using git push origin <tagname> we can push the tag(version) for local to remote repo.